# Three nodes so "Traefik landed on the Gerbil node" is a real assertion rather than the
# only possible outcome.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
