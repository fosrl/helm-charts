# Minimal values with existingSecret credentials
newtInstances:
  - name: main-tunnel
    pangolinEndpoint: "https://pangolin.example.com"
    auth:
      existingSecretName: newt-auth
    enabled: true
