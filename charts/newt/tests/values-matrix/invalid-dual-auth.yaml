# Negative test: both inline secret and external auth (should fail rendering)
newtInstances:
  - name: dual-auth
    enabled: true
    pangolinEndpoint: https://pangolin.example.com
    id: my-id
    secret: inline-secret
    auth:
      existingSecretName: external-secret
